// Session validation script - manual testing utility
// Note: This is a diagnostic utility, not a unit test

import { createSession, getSession, destroySession } from '../src/lib/auth';

console.log('Session validation utility loaded.');
console.log('To test sessions, use the login flow in the browser.');
console.log('Session functions available: createSession, getSession, destroySession');
